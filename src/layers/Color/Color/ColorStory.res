type color = StoryBook.argType<ColorReflection.t>;
let color = {
  "options": ColorReflection.args,
  "control": { "type": "select" },
}

type argTypes = {
  "color": color,
}

let argTypes = {
  "color": color,
}
