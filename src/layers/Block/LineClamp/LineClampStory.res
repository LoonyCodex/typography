type lineClamp = StoryBook.argType<LineClamp.t>
let lineClamp = {
  "options": LineClamp.args,
  "control": { "type": "select" },
}

type argTypes = {
  "lineClamp": lineClamp,
}

let argTypes = {
  "lineClamp": lineClamp,
}
