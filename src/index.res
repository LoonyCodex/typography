// Code
@genType.as("Code")
let codeSet = CodeSet.make
@genType.as("code")
let codeSetClassName = CodeSet.className

@genType.as("CodeInline")
let code = Code.make
@genType.as("codeInline")
let codeClassName = Code.className

@genType.as("Keyboard")
let kbd = Kbd.make
@genType.as("keyboard")
let kbdClassName = Kbd.className

@genType.as("CodeOutput")
let samp = Samp.make
@genType.as("codeOutput")
let sampClassName = Samp.className

@genType.as("Variable")
let var = Var.make
@genType.as("variable")
let varClassName = Var.className

// Heading
@genType.as("Heading")
let headingSet = HeadingSet.make
@genType.as("heading")
let headingSetClassName = HeadingSet.className

@genType.as("H1")
let h1 = H1.make
@genType.as("h1")
let h1ClassName = H1.className

@genType.as("H2")
let h2 = H2.make
@genType.as("h2")
let h2ClassName = H2.className

@genType.as("H3")
let h3 = H3.make
@genType.as("h3")
let h3ClassName = H3.className

@genType.as("H4")
let h4 = H4.make
@genType.as("h4")
let h4ClassName = H4.className

@genType.as("H5")
let h5 = H5.make
@genType.as("h5")
let h5ClassName = H5.className

@genType.as("H6")
let h6 = H6.make
@genType.as("h6")
let h6ClassName = H6.className

// LineBreak
@genType.as("LineBreak")
let lineBreak = LineBreak.make

// LineBreakOpportunity
@genType.as("LineBreakOpportunity")
let lineBreakOpportunity = LineBreakOpportunity.make

// Link
@genType.as("LinkSet")
let linkSet = LinkSet.make
@genType.as("linkSet")
let linkSetClassName = LinkSet.className

@genType.as("Link")
let a = A.make
@genType.as("link")
let aClassName = A.className

// Text
@genType.as("Text")
let text = TextSet.make
@genType.as("text")
let textClassName = TextSet.className

@genType.as("Abbreviation")
let abbreviation = Abbr.make
@genType.as("abbreviation")
let abbreviationClassName = Abbr.className

@genType.as("Attention")
let attention = B.make
@genType.as("attention")
let attentionClassName = B.className

@genType.as("Citation")
let citation = Cite.make
@genType.as("citation")
let citationClassName = Cite.className

@genType.as("Data")
let data = Data.make
@genType.as("data")
let dataClassName = Data.className

@genType.as("DefinedTerm")
let definedTerm = Dfn.make
@genType.as("definedTerm")
let definedTermClassName = Dfn.className

@genType.as("StressEmphasis")
let stressEmphasis = Em.make
@genType.as("stressEmphasis")
let stressEmphasisClassName = Em.className

@genType.as("FigCaption")
let figCaption = Figcaption.make
@genType.as("figCaption")
let figCaptionClassName = Figcaption.className

@genType.as("Idiomatic")
let idiomatic = I.make
@genType.as("idiomatic")
let idiomaticClassName = I.className

@genType.as("Highlight")
let highlight = Mark.make
@genType.as("highlight")
let highlightClassName = Mark.className

@genType.as("Outdated")
let outdated = S.make
@genType.as("outdated")
let outdatedClassName = S.className

@genType.as("Disclaimer")
let disclaimer = Small.make
@genType.as("disclaimer")
let disclaimerClassName = Small.className

@genType.as("TextInline")
let textInline = Span.make
@genType.as("textInline")
let textInlineClassName = Span.className

@genType.as("Important")
let important = Strong.make
@genType.as("important")
let importantClassName = Strong.className

@genType.as("Subscript")
let subscript = Sub.make
@genType.as("subscript")
let subscriptClassName = Sub.className

@genType.as("Superscript")
let superscript = Sup.make
@genType.as("superscript")
let superscriptClassName = Sup.className

@genType.as("UnarticulatedAnnotation")
let unarticulatedAnnotation = U.make
@genType.as("unarticulatedAnnotation")
let unarticulatedAnnotationClassName = U.className

// TextContainer
@genType.as("TextContainer")
let textContainer = TextContainerSet.make
@genType.as("textContainer")
let textContainerClassName = TextContainerSet.className

@genType.as("Paragraph")
let paragraph = P.make
@genType.as("paragraph")
let paragraphClassName = P.className

// Time
@genType.as("TimeSet")
let timeSet = TimeSet.make
@genType.as("timeSet")
let timeSetClassName = TimeSet.className

@genType.as("Time")
let time = Time.make
@genType.as("time")
let timeClassName = Time.className
